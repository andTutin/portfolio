- var skills = [['frontend', ['HTML5', 'CSS3', 'JavaScript', 'JQuery & Vue.js'], ['25', '13', '75', '25']],['workflow', ['GIT', 'Terminal', 'Gulp', 'Webpack'], ['13', '25', '50', '35']]]
ul.skills__list
    each item in skills
        li.skills__item(class=`${item[0]}`)
            h3.skills__item-title #{item[0]}
            ul(class=`${item[0]}__list`)
                each skill, index in item[1]
                    - var dashArray = item[2][index] * 2 * 3.14 * 80 / 100
                    li(class=`${item[0]}`+'__item')
                        .skill(class='skill--' + `${skill}`)
                            svg(viewBox="0 0 200 200" preserveAspectRatio="none").skill__shape
                                circle(cx="100" cy="100" r="80" stroke="#e9e9e9" stroke-width="10" fill="#fff")
                                circle(cx="100" cy="100" r="80" stroke="#3965ea" stroke-width="20" stroke-dasharray =`${dashArray}` + ' 503' fill="transparent" transform="rotate(-180 100 100)")
                            h4.skill__title= `${skill}`